{
  "name": "@ar-replanteo/importer",
  "version": "0.1.0",
  "description": "Standalone ASCII DXF importer for AR Replanteo CAD projects.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run build:deps && tsc -p tsconfig.json",
    "test": "npm run build && tsc -p tsconfig.test.json && node --test dist-test/test/**/*.test.js",
    "build:deps": "npm run build --prefix ../cad-engine && npm run build --prefix ../geometry-engine"
  },
  "dependencies": {
    "@ar-replanteo/cad-engine": "file:../cad-engine",
    "@ar-replanteo/geometry-engine": "file:../geometry-engine"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
