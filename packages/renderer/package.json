{
  "name": "@ar-replanteo/renderer",
  "version": "0.1.0",
  "description": "Standalone Three.js renderer for AR Replanteo CAD entities.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run build:deps && tsc -p tsconfig.json",
    "test": "npm run build && tsc -p tsconfig.test.json && node --test dist-test/test/**/*.test.js",
    "build:deps": "npm run build --prefix ../geometry-engine && npm run build --prefix ../cad-engine"
  },
  "dependencies": {
    "@ar-replanteo/cad-engine": "file:../cad-engine",
    "@ar-replanteo/geometry-engine": "file:../geometry-engine",
    "three": "^0.167.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "@types/three": "^0.167.0"
  }
}
